---
type: meeting
date: 2026-06-26
---
Projet : [[FeedGen Catégorisation]]

## Contexte

Nouvelle session après résolution des erreurs ModuleNotFoundError et passage en batch du `search.py`. On teste la V1 sur de vrais titres produits, puis on refactorise et structure le repo.

## Ce qu'on a fait

### Tests V1 — résultats décevants

Exécution de `test_rag.py` sur 5 titres bruts (fournitures de bureau Fellowes, EPI chantier). Résultats mauvais : "Support moniteur - noir" → moniteurs cardiaques, "agrafes" → albums photo. Cause : pas de `task_type` + bruit des marques/couleurs/tailles.

### V2 — Re-embedding avec RETRIEVAL_DOCUMENT

Création de `scripts/build_taxonomy_index.py` : ré-embed taxonomie FR+EN avec `task_type=RETRIEVAL_DOCUMENT`, upload GCS (`feedgen/taxonomy-embeddings-retrieval-doc/`), création index Vertex AI `feedgen-taxonomy-retrieval-doc`, déploiement sur le même endpoint (`feedgen_taxonomy_retrieval_doc`). Mise à jour `search.py` : `task_type=RETRIEVAL_QUERY` sur les requêtes.

Résultats V2 : encore pires que V1. Conclusion : RETRIEVAL_QUERY/DOCUMENT est conçu pour Q&A, pas pour matching titre court ↔ chemin taxonomie court. Le levier task_type n'est pas le bon ici.

### Extraction vers nouveau repo propre

Création de `C:\dev\DS\feedgen-categorisation-rag` (git init) avec uniquement les fichiers Adam (zéro dépendance sur le code de Dan) :
- `rag/search.py`, `rag/classify.py`
- `experiments/v1_baseline.py`, `experiments/v2_retrieval_doc.py`
- `scripts/` (infra + compare + analyze)
- `pyproject.toml` allégé (12 dépendances vs 25)

### Refactorisation architecture

- `rag/search.py` rendu paramétrique : `deployed_index_id` et `task_type` en params
- Chaque experiment expose `run()`, `run_with_names()`, `classify()`, et un `__main__` avec `--classify` / `--feedgen` flags
- `test_rag.py` supprimé — remplacé par `python experiments/v1_baseline.py`
- `results/` supprimé (résultats passent par BQ)

### Tables BQ de test

Deux tables créées dans `feed_manager_categorization` (LIMIT 1000, schema unifié avec colonne `title`) :
- `tb_test_raw_titles` ← `feed_manager.GVAtS5YBkqUxz5JwM0Ze_57fc_original` / `Title`
- `tb_test_feedgen_titles` ← `feed_manager.GVAtS5YBkqUxz5JwM0Ze_57fc_extended` / `_1a6c_Title`

Colonnes résultats : `predicted_category_id`, `predicted_category` (chemin complet), `predicted_score`, `category_changed` (True si level-2 prédit ≠ GMC_Level_2_Name existant).

### Classification V1 sur raw titles

`python experiments/v1_baseline.py --classify` lancé sur `tb_test_raw_titles` (1000 produits).

### Script d'analyse

`scripts/analyze_results.py` : 7 sections (distribution category_changed, distribution scores, avg score par changed, top 10 best/worst, catégories les plus prédites, taux de changement par GMC level-2). Configurable via `--raw` / `--feedgen` / `--table`.

## Décisions

- V7 (RAG level-by-level) abandonnée
- V5 (nettoyage titres) testée en dernier
- Ne jamais exécuter de script BQ sans accord explicite de l'utilisateur
- Prochaine priorité : V3 (LLM reranker top-5)

## Prochaine action

Analyser les résultats V1 sur raw titles, puis lancer classify sur feedgen titles pour comparer.
